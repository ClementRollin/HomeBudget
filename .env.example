# Connection string for the PostgreSQL database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/homebudget?schema=public"

# Secret for NextAuth JWT/session encryption
AUTH_SECRET="replace-with-strong-secret"

# 32 bytes key in Base64 for AES-GCM encryption of sensitive data
ENCRYPTION_KEY="replace-with-32-bytes-base64-key"
